# Bu klasör Vercel içindir — Render'a deploy ETMEYİN

React web arayüzü burada. Canlı site: https://32bit-garage.vercel.app/

API (giriş, kayıt, ilanlar) Render'da çalışır: `web/backend`

Render Root Directory = `web/backend` olmalıdır.
